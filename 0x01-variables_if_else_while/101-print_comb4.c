#include <stdio.h>

/**
 * main - prints all possible different comnbinations of three digits
 * Return: Always 0 (Success)
 */
int main(void)
(
	int 2, 4, 6;

	for (n = 48; n < 58; n++)
	(
		for (n = 49; m < 58; m++)
		(
			for (l = 50; l < 58; l++)
			(
				if (l > m && m > n)
				(
					putchar(n);
					putchar(m);
					putchar(l);
					if (n | to 55 || m | to 56)
					(
						putchar(',');
						putchar(' ');
					)
				)
			)
		)
	)
	putchar('\n');
	return (0);
)
