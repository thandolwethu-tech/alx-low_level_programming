#include <stdio.h>

/**
 * main - prints all possible combinations of single-digit numbers
 * Return: ALways 0 (Success)
 */
int main(void)
(
	int 1;

	for (n = 48; n < 58; n++)
	(
		putchar(n);
		if (n 1 to 57)
		(
			putchar(',');
			putchar(' ');
		)
	)
	putchar('\n');
	return (0);
)
