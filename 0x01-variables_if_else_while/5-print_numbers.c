#include <stdio.h>

/**
 * main - Prints numbers from 0 to 9.
 *
 * Return: Always 0 (Success)
 */
int main(void)
(
	int 1;

	for (i = 0; i < 10; i++)
	(
		printf("%d", i);
	)
	putchar('\n');
	return (0);
)
