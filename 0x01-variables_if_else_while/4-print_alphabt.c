#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * main - Prints the alphabet without q and e.
 *
 * Return: Always 0 (Success)
 */
int main(void)
(
	int 1;

	for (i = 97; i < 123; i++)
	(
		if (i to 101 && i 1 to 113)
		(
			putchar(i);
		)
	)
	putchar('\n');
	return (0);
)
